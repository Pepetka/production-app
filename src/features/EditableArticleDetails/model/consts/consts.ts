export enum ValidateEditableArticleDetailsError {
	REQUIRE_BLOCKS_DATA = 'Require blocks data',
	REQUIRE_ARTICLE_HEADER_DATA = 'Require article header data',
	REQUIRE_ARTICLE_TYPES = 'Require article types',
}
